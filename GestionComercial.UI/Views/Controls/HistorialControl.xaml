<UserControl x:Class="GestionComercial.UI.Views.Controls.HistorialControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:converters="clr-namespace:GestionComercial.UI.Views.Converters">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:DecimalToMonedaConverter  x:Key="DecimalToMoneda"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header con título y botón cerrar -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="📋" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Historial de Ventas" FontSize="18" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
            </StackPanel>
            <Button Grid.Column="1" Click="CerrarHistorial_Click"
                    Background="Transparent" BorderThickness="0"
                    Foreground="{DynamicResource TextSecondaryBrush}"
                    Cursor="Hand" Padding="8" ToolTip="Cerrar">
                <TextBlock Text="✕" FontSize="16"/>
            </Button>
        </Grid>
        
        <!-- Filtros de historial -->
        <Border Grid.Row="0" Margin="0,40,0,0" Background="{DynamicResource SurfaceBrush}" 
                CornerRadius="8" Padding="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Desde -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Desde" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <DatePicker SelectedDate="{Binding FechaDesde, Mode=TwoWay}" 
                               Style="{StaticResource FilterDatePickerStyle}"
                               SelectedDateChanged="FechaDesde_SelectedDateChanged"/>
                </StackPanel>

                <!-- Hasta -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Hasta" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <DatePicker SelectedDate="{Binding FechaHasta, Mode=TwoWay}"
                               Style="{StaticResource FilterDatePickerStyle}"
                               SelectedDateChanged="FechaHasta_SelectedDateChanged"/>
                </StackPanel>

                <!-- DNI Cliente -->
                <StackPanel Grid.Column="4">
                    <TextBlock Text="DNI Cliente" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox x:Name="DniClienteFiltroTextBox"
                             Style="{StaticResource FilterTextBoxStyle}"
                             Text="{Binding DniClienteFiltro, UpdateSourceTrigger=PropertyChanged}"
                             KeyDown="DniClienteFiltro_KeyDown">
                    </TextBox>
                </StackPanel>

                <!-- Estado -->
                <StackPanel Grid.Column="6">
                    <TextBlock Text="Estado" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ComboBox x:Name="EstadoFiltroCombo"
                              Height="44"
                              Style="{StaticResource FilterComboBoxStyle}"
                              SelectedValue="{Binding EstadoVentaFiltro, Mode=TwoWay}"
                              SelectedValuePath="Tag"
                              SelectionChanged="EstadoFiltro_SelectionChanged">
                        <ComboBoxItem Content="Todos" Tag="{x:Null}"/>
                        <ComboBoxItem Content="Pendiente" Tag="1"/>
                        <ComboBoxItem Content="Pagada" Tag="2"/>
                        <ComboBoxItem Content="Anulada" Tag="3"/>
                    </ComboBox>
                </StackPanel>

                <!-- Botón Filtrar -->
                <Button Grid.Column="8" Content="🔍 Filtrar"
                        Command="{Binding FiltrarHistorialCommand}"
                        Height="40" Padding="20,0"
                        Style="{StaticResource PrimaryButton}"
                        VerticalAlignment="Bottom"
                        ToolTip="Aplicar filtros"/>
            </Grid>
        </Border>
        
        <!-- DataGrid con estilo mejorado -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding HistorialVentas}" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False"
                  Background="Transparent" BorderThickness="0"
                  Foreground="{DynamicResource TextPrimaryBrush}"
                  GridLinesVisibility="None"
                  AlternationCount="2"
                  RowBackground="{DynamicResource CardBrush}"
                  AlternatingRowBackground="{DynamicResource SurfaceBrush}"
                  HeadersVisibility="Column"
                  CanUserReorderColumns="False"
                  SelectionMode="Single"
                  FontSize="13" Margin="0,70,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="FECHA" Binding="{Binding Fecha, StringFormat='dd/MM/yyyy HH:mm'}" Width="140">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="8,0"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="TOTAL" Binding="{Binding TotalFinal, StringFormat='$ {0:N2}'}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="8,0"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="CLIENTE" Binding="{Binding ClienteNombre}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="8,0"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="ESTADO" Binding="{Binding Estado}" Width="90">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="8,0"/>
                            <Setter Property="FontWeight" Value="Medium"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <!-- Columna de acciones con botón Anular -->
                <DataGridTemplateColumn Header="" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🗑 Anular"
                                    FontSize="11"
                                    Padding="8,4"
                                    Background="Transparent"
                                    Foreground="{DynamicResource ErrorBrush}"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    cal:Message.Attach="[Event Click] = [Action AnularVentaDesdeHistorial($dataContext)]"
                                    ToolTip="Anular esta venta">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="b" Background="{TemplateBinding Background}"
                                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="b" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Botón cerrar -->
        <Button Grid.Row="2" Content="Cerrar" Click="CerrarHistorial_Click"
                HorizontalAlignment="Right" Margin="0,16,0,0"
                Padding="24,10" FontSize="13"
                Background="{DynamicResource PrimaryBrush}"
                Foreground="{DynamicResource BackgroundBrush}"
                Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>