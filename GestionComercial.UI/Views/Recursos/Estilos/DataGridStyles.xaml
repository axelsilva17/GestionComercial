<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <SolidColorBrush x:Key="ColorFondoSecundarioBrush" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="ColorPrimarioLightBrush" Color="#FFCCE5FF"/>

    <SolidColorBrush x:Key="ColorPrimarioBrush" Color="#FF2196F3"/>

    <SolidColorBrush x:Key="ColorFondoPrincipalBrush" Color="#FFFFFFFF"/>

    <SolidColorBrush x:Key="ColorBordeBrush" Color="#FFE0E0E0"/>
    <SolidColorBrush x:Key="ColorTextoPrincipalBrush" Color="#FF212121"/>
    <FontFamily x:Key="FuentePrincipal">Segoe UI</FontFamily>

    <!-- Estilo para DataGrid -->
    <Style x:Key="DataGridModerno" TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource ColorFondoPrincipalBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ColorBordeBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource ColorFondoSecundarioBrush}"/>
        <Setter Property="RowHeight" Value="48"/>
        <Setter Property="FontFamily" Value="{StaticResource FuentePrincipal}"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- Estilo para las celdas del header -->
    <Style x:Key="DataGridColumnHeaderModerno" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource ColorFondoSecundarioBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ColorTextoPrincipalBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="12,12"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{StaticResource ColorBordeBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- Estilo para las celdas -->
    <Style x:Key="DataGridCellModerno" TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource ColorTextoPrincipalBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource ColorPrimarioLightBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource ColorPrimarioBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Estilo para las filas -->
    <Style x:Key="DataGridRowModerno" TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ColorFondoSecundarioBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource ColorPrimarioLightBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>