<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Fondos -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="#0F172A"/>
    <!-- Fondo principal oscuro -->
    <SolidColorBrush x:Key="CardBrush" Color="#1E293B"/>
    <!-- Tarjetas / sidebar -->
    <SolidColorBrush x:Key="SurfaceBrush" Color="#243447"/>
    <!-- Superficie secundaria -->

    <!-- Texto -->
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#E0F2FE"/>
    <!-- Texto principal claro -->
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#64748B"/>
    <!-- Texto secundario -->
    <SolidColorBrush x:Key="TextTertiaryBrush" Color="#38BDF8"/>
    <!-- Texto terciario / secciones -->

    <!-- Bordes y hover -->
    <SolidColorBrush x:Key="BorderBrush" Color="#2A3D52"/>
    <SolidColorBrush x:Key="HoverBrush" Color="#1E3A5F"/>

    <!-- Acciones / colores principales -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="#38BDF8"/>
    <!-- Color primario botones activos -->
    <SolidColorBrush x:Key="PrimaryAlphaBrush" Color="#3338BDF8"/>
    <!-- Primary con alfa 20% para seleccion en grids -->
    <SolidColorBrush x:Key="ErrorBrush" Color="#EF4444"/>
    <!-- Color error / cerrar -->

    <SolidColorBrush x:Key="SuccessBrush" Color="#10B981"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#F59E0B"/>
    <SolidColorBrush x:Key="InfoBrush" Color="#3B82F6"/>
    <SolidColorBrush x:Key="LightBackgroundBrush" Color="#F1F5F9"/>
    <SolidColorBrush x:Key="DarkBackgroundBrush" Color="#0F1419"/>

    <SolidColorBrush x:Key="PositiveBrush"  Color="#4ADE80"/>
    <SolidColorBrush x:Key="DangerBrush"    Color="#F87171"/>
    <SolidColorBrush x:Key="NeutralBrush"   Color="#94A3B8"/>
    <SolidColorBrush x:Key="GlassSurface"   Color="#0E1929"/>
</ResourceDictionary>
